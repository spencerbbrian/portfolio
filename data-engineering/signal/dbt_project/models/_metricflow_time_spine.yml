version: 2

models:
  - name: metricflow_time_spine
    description: "Required day-granularity time spine for the dbt Semantic Layer / MetricFlow."
    time_spine:
      standard_granularity_column: date_day
    columns:
      - name: date_day
        granularity: day
